# Invalid settings fixture - missing required 'model' field
workspaces_root: "test_workspaces/"

searxng_url: "http://localhost:8080"

whisper:
  model_size: "base"
  device: "cpu"
  compute_type: "int8"

embedding_model: "sentence-transformers/all-MiniLM-L6-v2"
