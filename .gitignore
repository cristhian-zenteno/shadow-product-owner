# --- Secrets and local credentials (never commit) ---
.kiro
.env
.env.*
!.env.example
.streamlit/secrets.toml
secrets.toml
credentials.json
*.pem
*.key
*.p12
*.pfx

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# --- Virtual environments ---
.venv/
venv/
ENV/
env/

# --- Testing and coverage ---
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/

# --- Type checkers and linters ---
.mypy_cache/
.ruff_cache/
.pytype/

# --- Runtime workspaces (documents, transcripts, chat, vector indexes) ---
workspaces/*
!workspaces/.gitkeep

# Per-workspace Chroma persistence (if created outside workspaces/)
**/.chroma/

# --- ML / model caches (if downloaded into the project) ---
models/
*.pt
*.onnx
*.bin
*.safetensors

# --- Streamlit ---
.streamlit/cache/

# --- OS and editor ---
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# --- Logs and temp files ---
*.log
logs/
tmp/
temp/

# --- uv (lockfile is committed; local env is not) ---
# uv.lock is intentionally tracked for reproducible installs
